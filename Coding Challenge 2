btns = document.querySelectorAll('[href="/\#signup"]');
    for (var i = 0; i < btns.length; i++) {
        btns[i].setAttribute("btnnodeposition", i+1);
		btns[i].addEventListener("click", function () {
			alert ("Clicked button "+ this.getAttribute("btnnodeposition"));
        });
    }
